#!/bin/bash -eu

if [ $# -eq 0 ] || [ -z "$1" ]; then
    echo 'Please provide a directory!' 1>&2
    exit 1
fi

~/script/linux/find-music "$1" | {
    files=()

    while read line; do
        files+=("$line")
    done

    xmms2 add "${files[@]}"
}
